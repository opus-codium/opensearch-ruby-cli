# frozen_string_literal: true

RSpec.describe Opensearch::Ruby::Cli do
  it "has a version number" do
    expect(Opensearch::Ruby::Cli::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
